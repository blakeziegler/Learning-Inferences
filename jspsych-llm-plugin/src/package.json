{
  "name": "summarize-grader",
  "version": "1.0.0",
  "description": "A jsPsych plugin for summarizing text and grading summaries",
  "main": "/jspsych-llm-plugin/src/SummaryGrader.ts",
  "scripts": {
    "build": "../node_modules/.bin/tsc && rollup --config",
    "build:watch": "../node_modules/.bin/tsc --watch & rollup --config --watch"
  },
  "devDependencies": {
    "@jspsych/config": "^3.0.0",
    "@jspsych/test-utils": "^1.2.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.43.5",
    "jest": "^27.0.0",
    "typescript": "4.9.5"
  },
  "dependencies": {
    "glob": "^11.0.0",
    "jspsych": "^7.0.0"
  }
}
